var readlineSync = require('readline-sync');
var utility = require('../Utility/algUtility.js')
function game()
{
     var N=readlineSync.question("enter the N value ");
     var n=readlineSync.question(`select 1 number from 0 to ${(N-1)}`)
     if(n>N)
        {

        }
}